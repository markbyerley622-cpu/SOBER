# Solana Configuration
# Use 'devnet' for development, 'mainnet-beta' for production
NEXT_PUBLIC_SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional: Custom RPC endpoint (recommended for production)
# NEXT_PUBLIC_SOLANA_RPC_URL=https://your-custom-rpc.com

# $SOBER Token Mint Address (replace with actual mint address)
NEXT_PUBLIC_SOBER_TOKEN_MINT=your_token_mint_address

# API Configuration (for future backend expansion)
# DATABASE_URL=your_database_url
# IPFS_API_KEY=your_ipfs_api_key
